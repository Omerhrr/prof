import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PROF Charity & Care Foundation | Transforming Lives in Northern Nigeria",
  description:
    "PROF Charity & Care Foundation is dedicated to providing relief, food, and essential supplies to underserved communities across Northern Nigeria, primarily in Kano State.",
  keywords: [
    "PROF Charity",
    "Care Foundation",
    "Northern Nigeria",
    "Kano",
    "Charity",
    "NGO",
    "Food Distribution",
    "Community Support",
  ],
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
